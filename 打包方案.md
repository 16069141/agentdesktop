# 私有域 AI 助手 — 桌面客户端打包方案

> 版本：v1.0  
> 日期：2026-09-07  
> 适用项目：`v2/`（Electron + Python FastAPI 混合架构）

---

## 一、项目架构概览

```
v2/
├── desktop/              # Electron 前端（渲染层 + 主进程）
│   ├── src/              # React 18 + TypeScript + Tailwind + ECharts
│   ├── electron/         # 主进程：main.ts / agentProcess.ts / preload.ts
│   ├── vite.config.ts    # Vite 构建（含 vite-plugin-electron）
│   └── electron-builder.yml  # 打包配置
├── agent/                # Python 后端（FastAPI + Uvicorn）
│   ├── app/              # 业务代码（api / agents / connectors / ...）
│   ├── .venv/            # Python 虚拟环境（326 MB）
│   └── requirements.txt  # fastapi / uvicorn / openai / keyring / ...
├── config/settings.json  # 运行时配置
├── scripts/              # 根级辅助脚本（含旧版 package.sh）
└── release/              # 打包产物输出目录
```

**运行时通信模型：**

```
Electron 主进程
  └─ spawn() → Python 子进程（python -m app.main）
                  └─ Uvicorn 监听 127.0.0.1:8765
                       └─ 渲染进程通过 fetch + 随机 Token 调用 REST API
```

关键设计点：
- Python 后端以**子进程**形式由 Electron 拉起，退出时自动回收
- 每次启动生成一次性随机 Token（32 字节 hex），仅本地回环有效
- 打包态数据目录落在 `app.getPath('userData')/agent-data`，避免写入只读的 `.app` 资源目录
- 主进程已实现 `isPackaged` 路径切换（开发态走仓库 `agent/`，打包态走 `Contents/Resources/agent`）

---

## 二、当前打包方案分析

### 2.1 已实现的能力

| 环节 | 实现方式 | 状态 |
|------|---------|------|
| 前端构建 | `tsc && vite build` → `dist/` + `dist-electron/` | ✅ 可用 |
| 后端暂存 | `scripts/stage-release.sh` 把 `agent/` + `.venv/` rsync 到 `build-staging/agent` | ✅ 可用 |
| 打包工具 | electron-builder 25.1.8 | ✅ 可用 |
| 资源注入 | `extraResources` 将 `build-staging/agent` 打入 `Contents/Resources/agent` | ✅ 可用 |
| macOS DMG | 已产出 `私有域AI助手-0.1.0-arm64.dmg`（198 MB） | ✅ 已验证 |
| Windows NSIS | 配置存在，未在本机验证 | ⚠️ 未验证 |
| Linux AppImage | 配置存在，未验证 | ⚠️ 未验证 |

### 2.2 存在的问题

**问题 1：venv 直接拷贝 — 跨平台/跨架构不可移植**

当前 `stage-release.sh` 直接把 `.venv/` 整个 rsync 进安装包。这在**同架构同 OS** 的机器上能跑，但有硬伤：

- venv 中的 `pyvenv.cfg` 和脚本 shebang 写死了创建时的绝对路径（如 `/Users/caojian/.../python`），拷贝到其他用户机器后路径失效
- arm64 构建的 venv 无法在 x64 机器上运行（含编译型依赖如 `pydantic-core`、`uvloop`）
- macOS 构建的 venv 无法在 Windows/Linux 上运行
- 体积庞大：venv 占 326 MB，是安装包体积的主要来源

**问题 2：缺少应用图标**

`desktop/build/` 目录为空，当前打包使用 Electron 默认图标。需要准备：
- `icon.icns`（macOS，1024×1024 源图）
- `icon.ico`（Windows，多尺寸）
- `icon.png`（Linux，512×512）

**问题 3：未做代码签名与公证**

macOS 上未签名的 `.app` 会被 Gatekeeper 拦截，用户需右键"打开"才能运行；Windows 上未签名的安装包会触发 SmartScreen 警告。

**问题 4：两套打包配置并存，容易混淆**

- `desktop/electron-builder.yml`（实际生效，JSON 格式）
- `scripts/package.sh`（根目录旧脚本，运行时会**覆盖** `electron-builder.yml`，且配置项与实际不一致，如包含 `publish`、`hardenedRuntime` 等未使用字段）

**问题 5：Python 3.13 较新**

部分依赖在 Python 3.13 上可能没有预编译 wheel，导致安装时需要编译。建议锁定到 Python 3.11 或 3.12（LTS 更稳，依赖兼容性更好）。

**问题 6：Windows 打包无法在 macOS 上直接完成**

electron-builder 在 macOS 上构建 Windows 安装包需要 Wine，且 NSIS 打包质量不如在 Windows 原生环境。推荐使用 CI 矩阵（macOS runner 打 mac，Windows runner 打 win）。

---

## 三、推荐打包方案

### 3.1 方案选型：Python 后端打包策略对比

| 策略 | 原理 | 体积 | 跨架构 | 启动速度 | 维护成本 | 推荐度 |
|------|------|------|--------|---------|---------|--------|
| **A. venv 内嵌**（当前） | 直接拷贝 `.venv/` 到 Resources | ~330 MB | ❌ 同架构 only | 快（直接 python） | 低 | ⭐⭐ 同机分发可用 |
| **B. PyInstaller 单文件** | 把 Python + 依赖 + 代码编译成单个可执行文件 | ~80–150 MB | ✅ 每平台各打一次 | 慢（首次解压 ~2–5s） | 中 | ⭐⭐⭐⭐ 推荐 |
| **C. PyInstaller 目录模式** | 编译成 `_internal/` 目录 + 可执行文件 | ~100–180 MB | ✅ 每平台各打一次 | 中 | 中 | ⭐⭐⭐ |
| **D. 嵌入式 Python + pip** | 随包带 python 嵌入版，首次运行 pip install | ~50 MB + 运行时下载 | ✅ | 首次慢 | 高 | ⭐⭐ 不推荐 |

**推荐：策略 B（PyInstaller 单文件）**，理由：
1. 彻底解决 venv 路径硬编码和跨架构问题
2. 安装包体积从 ~330 MB 降到 ~100 MB 以内
3. 用户无需安装 Python，开箱即用
4. Electron 主进程只需 spawn 一个二进制文件，路径逻辑更简单
5. 代码可混淆，一定程度保护后端源码

### 3.2 整体打包流水线

```
┌─────────────────────────────────────────────────────────────┐
│                     一键打包入口                              │
│              bash scripts/build-all.sh [mac|win|linux]      │
└──────────────────────┬──────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       ▼               ▼               ▼
  ┌─────────┐    ┌─────────┐    ┌──────────────┐
  │ 前端构建 │    │ 后端构建 │    │ 图标资源准备  │
  │ vite    │    │PyInstaller│   │ icon 生成    │
  │ build   │    │ --onefile │   │              │
  └────┬────┘    └────┬────┘    └──────┬───────┘
       │               │                 │
       └───────────────┼─────────────────┘
                       ▼
              ┌─────────────────┐
              │  stage 暂存区    │
              │  build-staging/  │
              │  ├─ agent-bin/   │  ← PyInstaller 产物
              │  └─ resources/   │  ← 配置、模型等
              └────────┬────────┘
                       ▼
              ┌─────────────────┐
              │ electron-builder │
              │  按平台打包       │
              └────────┬────────┘
                       ▼
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   ┌─────────┐   ┌──────────┐  ┌───────────┐
   │ macOS   │   │ Windows  │  │ Linux     │
   │ .dmg    │   │ .exe     │  │ .AppImage │
   │ +签名公证│   │ +签名     │  │           │
   └─────────┘   └──────────┘  └───────────┘
```

---

## 四、具体实施步骤

### 4.1 第一步：准备应用图标

在 `desktop/build/` 目录下放置图标源文件。推荐准备一张 1024×1024 的 PNG 源图，然后用工具转换：

```bash
# 安装图标生成工具（macOS）
brew install imagemagick iconutil

# 假设源图为 build/icon-1024.png
cd desktop/build

# macOS: 生成 icns
mkdir icon.iconset
sips -z 16 16     icon-1024.png --out icon.iconset/icon_16x16.png
sips -z 32 32     icon-1024.png --out icon.iconset/icon_16x16@2x.png
sips -z 32 32     icon-1024.png --out icon.iconset/icon_32x32.png
sips -z 64 64     icon-1024.png --out icon.iconset/icon_32x32@2x.png
sips -z 128 128   icon-1024.png --out icon.iconset/icon_128x128.png
sips -z 256 256   icon-1024.png --out icon.iconset/icon_128x128@2x.png
sips -z 256 256   icon-1024.png --out icon.iconset/icon_256x256.png
sips -z 512 512   icon-1024.png --out icon.iconset/icon_256x256@2x.png
sips -z 512 512   icon-1024.png --out icon.iconset/icon_512x512.png
cp icon-1024.png icon.iconset/icon_512x512@2x.png
iconutil -c icns icon.iconset -o icon.icns
rm -rf icon.iconset

# Windows: 生成 ico（多尺寸）
magick icon-1024.png -define icon:auto-resize=256,128,64,48,32,16 icon.ico

# Linux: 直接用 png
cp icon-1024.png icon.png
```

### 4.2 第二步：用 PyInstaller 打包 Python 后端

#### 4.2.1 安装 PyInstaller

```bash
cd agent
source .venv/bin/activate
pip install pyinstaller
```

#### 4.2.2 创建 PyInstaller 配置文件 `agent/agent.spec`

```python
# -*- mode: python ; coding: utf-8 -*-
"""
PyInstaller 配置：将 FastAPI 后端打包为单文件可执行程序。
用法：pyinstaller agent.spec
"""
import sys
from pathlib import Path

block_cipher = None
ROOT = Path(SPECPATH)

a = Analysis(
    ['app/main.py'],              # 入口
    pathex=[str(ROOT)],
    binaries=[],
    datas=[
        # 若有静态资源/模板，在此添加：(str(ROOT / 'app/static'), 'static')
    ],
    hiddenimports=[
        'uvicorn.logging',
        'uvicorn.loops',
        'uvicorn.loops.auto',
        'uvicorn.protocols',
        'uvicorn.protocols.http',
        'uvicorn.protocols.http.auto',
        'uvicorn.protocols.websockets',
        'uvicorn.protocols.websockets.auto',
        'uvicorn.lifespan',
        'uvicorn.lifespan.on',
        'aiosqlite',
        'keyring.backends',
        'keyring.backends.macOS',
        'keyring.backends.Windows',
        'openai',
        'pydantic',
        'pydantic_core',
        'fastapi',
        'starlette',
        'anyio',
        'h11',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[
        'tkinter',
        'test',
        'unittest',
        'pytest',
        'IPython',
        'jupyter',
        'matplotlib',
        'numpy',
        'scipy',
        'pandas',
        'PIL',
    ],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='agent',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,              # 启用 UPX 压缩（需安装 upx）
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,         # 不弹出控制台窗口（macOS/Linux 设为 True 便于调试）
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
```

#### 4.2.3 执行打包

```bash
cd agent
source .venv/bin/activate
pyinstaller agent.spec --clean --noconfirm
# 产物：agent/dist/agent（单文件可执行）
```

### 4.3 第三步：修改 Electron 主进程，适配 PyInstaller 产物

修改 `desktop/electron/agentProcess.ts` 中的 `resolvePython()` 和 `resolveAgentRoot()`：

```typescript
/** 解析后端可执行文件：打包态用 PyInstaller 产物，开发态用 venv python */
function resolveAgentBinary(): string {
  if (process.env.AGENT_BINARY) return process.env.AGENT_BINARY

  if (isPackaged) {
    // 打包后：agent 可执行文件在 Contents/Resources/agent-bin/agent
    const ext = process.platform === 'win32' ? '.exe' : ''
    const bundled = join(process.resourcesPath, `agent-bin/agent${ext}`)
    if (existsSync(bundled)) return bundled
  }

  // 开发态：用 venv python -m app.main
  const venvPython = join(__dirname, '../../agent/.venv/bin/python')
  if (existsSync(venvPython)) return venvPython
  return 'python3'
}

/** 打包态 cwd 设为 resourcesPath，开发态设为 agent 根目录 */
function resolveAgentCwd(): string {
  if (isPackaged) return process.resourcesPath
  return join(__dirname, '..', '..', 'agent')
}

export async function spawnAgentProcess(token: string, approvalUrl = ''): Promise<boolean> {
  if (agentProcess) stopAgentProcess()
  stopped = false

  const binary = resolveAgentBinary()
  const cwd = resolveAgentCwd()
  const isPython = binary.endsWith('python') || binary.endsWith('python3')

  // PyInstaller 产物直接执行；开发态用 python -m app.main
  const args = isPython ? ['-m', 'app.main'] : []
  console.log(`[Agent] 启动: ${binary} ${args.join(' ')} (cwd=${cwd})`)

  agentProcess = spawn(binary, args, {
    cwd,
    env: {
      ...process.env,
      AGENT_TOKEN: token,
      AGENT_APPROVAL_URL: approvalUrl,
      AGENT_PORT: String(AGENT_PORT),
      AGENT_HOST: '127.0.0.1',
      PYTHONUNBUFFERED: '1',
      ...(resolveDataDir() ? { AGENT_DATA_DIR: resolveDataDir() } : {}),
    },
    stdio: ['ignore', 'pipe', 'pipe'],
  })
  // ... 后续逻辑不变
}
```

### 4.4 第四步：更新 stage-release.sh

```bash
#!/usr/bin/env bash
# 打包前置：把 PyInstaller 产物与必要资源暂存到 build-staging/
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
AGENT_DIR="$(cd "$ROOT/.." && pwd)/agent"
STAGE="$ROOT/build-staging"

echo "[stage] 清理旧暂存区..."
rm -rf "$STAGE"
mkdir -p "$STAGE/agent-bin"

# 1) PyInstaller 后端可执行文件
AGENT_BIN="$AGENT_DIR/dist/agent"
if [ ! -f "$AGENT_BIN" ]; then
  echo "错误：未找到 PyInstaller 产物 $AGENT_BIN"
  echo "请先执行：cd agent && source .venv/bin/activate && pyinstaller agent.spec"
  exit 1
fi
cp "$AGENT_BIN" "$STAGE/agent-bin/agent"
chmod +x "$STAGE/agent-bin/agent"
echo "[stage] 后端可执行文件已复制: $(du -sh "$STAGE/agent-bin/agent" | cut -f1)"

# 2) 运行时配置（如有需要随包分发的默认配置）
CONFIG_DIR="$(cd "$ROOT/.." && pwd)/config"
if [ -d "$CONFIG_DIR" ]; then
  mkdir -p "$STAGE/config"
  cp "$CONFIG_DIR/settings.json" "$STAGE/config/" 2>/dev/null || true
fi

echo "[stage] 完成，暂存区总大小: $(du -sh "$STAGE" | cut -f1)"
```

### 4.5 第五步：更新 electron-builder.yml

```yaml
appId: com.private-ai.client
productName: 私有域AI助手
directories:
  buildResources: build
  output: ../release
files:
  - dist/**/*
  - dist-electron/**/*
extraResources:
  - from: build-staging/agent-bin
    to: agent-bin
  - from: build-staging/config
    to: config
    filter: ["**/*"]
mac:
  target:
    - target: dmg
      arch: [arm64, x64]
  icon: build/icon.icns
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: false
win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/icon.ico
linux:
  target:
    - target: AppImage
      arch: [x64]
  icon: build/icon.png
  category: Utility
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  createDesktopShortcut: true
  createStartMenuShortcut: true
```

### 4.6 第六步：统一打包入口脚本

创建 `scripts/build-all.sh`（替代旧的 `package.sh`）：

```bash
#!/usr/bin/env bash
# 统一打包入口：前端构建 → 后端 PyInstaller → 暂存 → electron-builder
# 用法: bash scripts/build-all.sh [mac|win|linux|all]
set -euo pipefail

PLATFORM=${1:-"mac"}
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
DESKTOP="$ROOT/desktop"
AGENT="$ROOT/agent"

echo "========================================"
echo "  私有域 AI 助手 — 打包构建"
echo "  平台: $PLATFORM"
echo "========================================"

# 1) 后端 PyInstaller 打包
echo ""
echo "[1/4] 打包 Python 后端 (PyInstaller)..."
cd "$AGENT"
if [ ! -d ".venv" ]; then
  python3 -m venv .venv
fi
source .venv/bin/activate
pip install --quiet --upgrade pip
pip install --quiet -r requirements.txt
pip install --quiet pyinstaller
pyinstaller agent.spec --clean --noconfirm
deactivate
echo "  ✓ 后端打包完成: $(du -sh dist/agent | cut -f1)"

# 2) 前端构建
echo ""
echo "[2/4] 构建前端 (Vite)..."
cd "$DESKTOP"
npm install --silent
npm run build
echo "  ✓ 前端构建完成"

# 3) 暂存资源
echo ""
echo "[3/4] 暂存打包资源..."
bash scripts/stage-release.sh

# 4) electron-builder 打包
echo ""
echo "[4/4] 执行 electron-builder ($PLATFORM)..."
case "$PLATFORM" in
  mac)   npx electron-builder --mac ;;
  win)   npx electron-builder --win ;;
  linux) npx electron-builder --linux ;;
  all)   npx electron-builder -mw ;;
  *)     echo "未知平台: $PLATFORM"; exit 1 ;;
esac

echo ""
echo "========================================"
echo "  打包完成！产物目录: $ROOT/release"
echo "========================================"
ls -lh "$ROOT/release" | grep -v '^total'
```

---

## 五、代码签名与公证

### 5.1 macOS 签名与公证（必须，否则 Gatekeeper 拦截）

**前置条件：**
- Apple Developer ID（$99/年）
- 已安装 Xcode Command Line Tools
- 已在 Keychain 中导入 Developer ID Application 证书

**在 `electron-builder.yml` 中添加：**

```yaml
mac:
  # ... 其他配置
  identity: "Developer ID Application: Your Company (TEAMID)"
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist

afterSign: scripts/notarize.js
```

**创建 `desktop/build/entitlements.mac.plist`：**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
</dict>
</plist>
```

**创建 `desktop/scripts/notarize.js`：**

```javascript
const { notarize } = require('@electron/notarize')

exports.default = async function notarizing(context) {
  const { electronPlatformName, appOutDir } = context
  if (electronPlatformName !== 'darwin') return

  const appName = context.packager.appInfo.productFilename
  const appPath = `${appOutDir}/${appName}.app`

  console.log(`[notarize] 提交公证: ${appPath}`)
  await notarize({
    appBundleId: 'com.private-ai.client',
    appPath,
    appleId: process.env.APPLE_ID,
    appleIdPassword: process.env.APPLE_APP_SPECIFIC_PASSWORD,
    teamId: process.env.APPLE_TEAM_ID,
  })
  console.log('[notarize] 公证完成')
}
```

**安装依赖并设置环境变量：**

```bash
cd desktop
npm install --save-dev @electron/notarize

# 打包前设置（不要写入代码仓库）
export APPLE_ID="your@email.com"
export APPLE_APP_SPECIFIC_PASSWORD="xxxx-xxxx-xxxx-xxxx"
export APPLE_TEAM_ID="TEAMID1234"
```

### 5.2 Windows 代码签名（推荐，消除 SmartScreen 警告）

**前置条件：**
- 购买代码签名证书（DigiCert / Sectigo，约 $200–500/年）
- 或使用 EV 证书（自动获得 SmartScreen 信誉，约 $300–700/年）

**在 `electron-builder.yml` 中添加：**

```yaml
win:
  # ... 其他配置
  certificateFile: build/certificate.pfx
  certificatePassword: "${WIN_CERT_PASSWORD}"
```

---

## 六、体积优化清单

| 优化项 | 预期收益 | 实施方式 |
|--------|---------|---------|
| venv → PyInstaller | -200 MB | 见第四章 |
| PyInstaller UPX 压缩 | -30~50% | `agent.spec` 中 `upx=True`，需 `brew install upx` |
| 排除无用 Python 包 | -20~50 MB | `excludes` 中列出 tkinter / test / matplotlib 等 |
| Electron asar 打包 | 默认开启 | electron-builder 默认 asar=true |
| 前端 tree-shaking | Vite 默认 | 已开启 |
| 移除 source map | -几 MB | `vite.config.ts` 中 `build.sourcemap: false` |
| dmg 压缩 | 默认 | electron-builder dmg 默认使用 UDZO 压缩 |
| 不打包 node_modules 源码 | 默认 | `files` 只含 dist，不含 node_modules |

**预期最终体积：**
- macOS DMG：~60–90 MB（当前 198 MB）
- Windows EXE：~70–100 MB
- Linux AppImage：~80–110 MB

---

## 七、CI/CD 建议（GitHub Actions 示例）

```yaml
# .github/workflows/release.yml
name: Build & Release

on:
  push:
    tags: ['v*']

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Build & Package
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: bash scripts/build-all.sh mac
      - uses: actions/upload-artifact@v4
        with:
          name: mac-dmg
          path: release/*.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Build & Package
        shell: bash
        run: bash scripts/build-all.sh win
      - uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: release/*.exe

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Install deps
        run: sudo apt-get install -y libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils
      - name: Build & Package
        run: bash scripts/build-all.sh linux
      - uses: actions/upload-artifact@v4
        with:
          name: linux-appimage
          path: release/*.AppImage
```

---

## 八、验证清单

打包完成后，按以下清单逐项验证：

### 8.1 功能验证

- [ ] 安装包能正常安装，无报错
- [ ] 应用能正常启动，主窗口显示
- [ ] Python 后端自动启动，`/healthz` 返回 200
- [ ] 聊天功能正常（发送/接收消息）
- [ ] 会话历史持久化（重启后数据保留）
- [ ] 文件上传/下载功能正常
- [ ] Shell 命令执行弹出确认对话框
- [ ] 最小化/最大化/关闭按钮正常
- [ ] 退出应用时 Python 子进程被正确回收（无残留进程）

### 8.2 路径与权限验证

- [ ] 打包态数据写入 `userData/agent-data`，不写入 `.app` 内部
- [ ] 配置文件读写正常
- [ ] macOS 首次启动无 Gatekeeper 拦截（签名后）
- [ ] Windows 安装无 SmartScreen 警告（签名后）
- [ ] 安装到非默认路径仍能正常运行

### 8.3 性能验证

- [ ] 冷启动时间 < 5 秒（从点击图标到界面可交互）
- [ ] 后端启动时间 < 10 秒（PyInstaller 首次解压可能较慢）
- [ ] 内存占用：Electron < 500 MB，Python < 200 MB
- [ ] 安装包体积符合预期（macOS < 100 MB）

### 8.4 兼容性验证

- [ ] macOS arm64（Apple Silicon）正常
- [ ] macOS x64（Intel）正常（如支持）
- [ ] Windows 10/11 x64 正常
- [ ] Linux（Ubuntu 20.04+）正常（如支持）

---

## 九、迁移路径（从当前方案到推荐方案）

如果希望**渐进式迁移**，可按以下顺序：

| 阶段 | 改动 | 风险 | 收益 |
|------|------|------|------|
| **阶段 0**（当前） | venv 内嵌 + 无图标 + 无签名 | 低 | 已可用，同机分发 |
| **阶段 1** | 补充应用图标 + 清理旧 package.sh | 极低 | 产品化基础 |
| **阶段 2** | macOS 代码签名 + 公证 | 低 | 消除 Gatekeeper 拦截 |
| **阶段 3** | PyInstaller 替换 venv 内嵌 | 中 | 体积 -60%，跨架构可用 |
| **阶段 4** | Windows 签名 + CI/CD 矩阵 | 中 | 全平台自动化发布 |
| **阶段 5** | 自动更新（electron-updater） | 中 | 支持在线升级 |

**建议：先完成阶段 1–2（图标 + 签名），这是产品发布的最低要求；阶段 3（PyInstaller）是体积和兼容性的关键改进，建议在版本稳定后实施。**

---

## 十、附录：关键文件速查

| 文件 | 作用 |
|------|------|
| `desktop/package.json` | 前端依赖与构建脚本 |
| `desktop/electron-builder.yml` | electron-builder 打包配置 |
| `desktop/electron/main.ts` | Electron 主进程入口 |
| `desktop/electron/agentProcess.ts` | Python 子进程管理（路径解析在此） |
| `desktop/scripts/stage-release.sh` | 打包前资源暂存 |
| `agent/agent.spec` | PyInstaller 配置（需新建） |
| `agent/app/main.py` | Python 后端入口 |
| `agent/requirements.txt` | Python 依赖清单 |
| `scripts/build-all.sh` | 统一打包入口（需新建） |
| `release/` | 打包产物输出目录 |

---

*方案结束。如有疑问或需要针对某个环节深入展开，请告知。*

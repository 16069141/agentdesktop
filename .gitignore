# ── 操作系统噪声 ─────────────────────────────
.DS_Store
._*
Thumbs.db
Desktop.ini

# ── Python ──────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 虚拟环境
.venv/
venv/
env/

# ── Node / Electron ─────────────────────────
node_modules/

# 构建产物（Vite / electron-builder / 打包暂存）
desktop/dist/
desktop/dist-electron/
desktop/build-staging/
release/

# ── 运行时数据（上传文件 / 生成文件 / 本地数据库）──
agent/data/
*.log

# ── 本地密钥/环境 ────────────────────────────
.env
.env.local

# ── 本地工具/编辑器数据（收件箱、会话缓存等）────
.dumate/

# 私有域 AI 助手 · 开发流程与省 Token 策略

> **文档用途**：基于《DEV-COMMAND-PROMPT.md》（指挥开发 Prompt）与《DEVELOPMENT-GUIDE.md》（开发规格书）梳理的端到端开发流程，重点解决 **「最省 Token」**——即用最少的模型调用成本驱动 AI 编码助手完成整个 MVP。
>
> **省 Token 的核心洞察**：AI 编码助手的费用主要由「上下文输入 token」与「返工次数」决定。因此策略是：**不全量投喂、按阶段切片、引用式加载、DoD 门禁防返工**。

---

## 一、开发流程总览（10–14 周，5 个阶段）

```
Phase 0           Phase 1           Phase 2            Phase 3             Phase 4
需求冻结+PoC  →   基础框架     →    核心能力     →     Agent化+编程助手  →   打磨发布
(1周)            (2–3周)          (3–4周)            (3–4周)             (2–3周)
```

| 阶段 | 名称 | 周期 | 核心产出 | 里程碑验收 |
|------|------|------|----------|------------|
| P0 | 需求冻结 + PoC | 1 周 | 冻结 SSE/API 协议；最小流式对话跑通 | curl 流式拿到 `meta/text/done` |
| P1 | 基础框架 | 2–3 周 | 桌面骨架 + 对话 UI + 后端骨架 + SSE 端到端 | 可建/删会话、流式回复、重启不丢（A1/A5） |
| P2 | 核心能力 | 3–4 周 | MCP + RAG + 上下文 + 钥匙串 + 安全基线 | RAG Top-5 ≥80%（A3）、安全验证（A6） |
| P3 | Agent 化 + 编程助手 | 3–4 周 | 多轮工具闭环 + 模型路由 + 编程助手 + 评测集 | 工具调用 ≥3 轮（A4） |
| P4 | 打磨发布 | 2–3 周 | 生命周期管理 + 打包 + 文档 + 全量验收 | 六项硬指标全过（A1–A6） |

> 阶段间存在强依赖：P0 冻结协议 → P1 前端后端并行（都依赖 P0 的契约）→ P2 依赖 P1 的后端骨架 → P3 依赖 P2 的 MCP/RAG → P4 收尾。

---

## 二、各阶段详述

### Phase 0 — 需求冻结 + PoC（1 周）

**目标**：把「要做什么、接口长什么样」一次性定死，避免后续返工。

**任务**：
- [ ] 冻结 §7 通信协议（SSE 事件 + HTTP 接口 + 错误码）
- [ ] 冻结 §6 数据模型（SQLite 表 + ChromaDB 集合 + 配置）
- [ ] PoC：Ollama 本地模型 + OpenAI 兼容接口跑通最小流式对话

**DoD**：curl 能流式拿到 `meta/text/done` 事件；协议表评审通过。

**省 Token 要点**：
- 这是**唯一一次全量评审**，协议/数据模型在此冻结后不再重复讨论——冻结后所有阶段只「引用」不再「重述」。
- PoC 用一个极简脚本验证链路，不搭完整工程。

---

### Phase 1 — 基础框架（2–3 周）

**目标**：把「能对话」的骨架立起来，前后端可并行。

**任务**：
- [ ] Electron + React 骨架（目录对齐规格书 §5）
- [ ] 基础对话 UI（消息列表、输入框、停止生成）+ 主题切换
- [ ] FastAPI 骨架：127.0.0.1 绑定 + token 鉴权 + /healthz
- [ ] SSE 客户端（粘包/半包处理）+ 流式对话端到端打通
- [ ] 会话持久化（SQLite）+ 会话列表 UI
- [ ] 模型配置与切换界面（含私有化与互联网商业大模型，公网模型带「公网」标识 + 知情提示）

**DoD**：新建/删除会话、发消息收流式回复、重启会话不丢（验收 A1/A5 部分）。

**省 Token 要点**：
- 前端只投喂「规格书 §8（前端规格）+ §5（目录）+ §7（协议）」切片，**不投喂后端实现细节**。
- 后端只投喂「§9.1/§9.2（Provider + Agent）+ §6（数据模型）+ §7（协议）」切片，**不投喂前端 UI 细节**。
- 前后端各自独立会话，互不污染上下文。

---

### Phase 2 — 核心能力（3–4 周）

**目标**：把「工具与知识库」能力补上，这是 MVP 的灵魂。

**任务**：
- [ ] MCP Manager：工具集启停、来源白名单、审计日志
- [ ] 内置工具：filesystem / shell（白名单 + 确认 + 超时）/ knowledge
- [ ] RAG：文档导入 + 混合检索（BM25 + 向量 RRF）+ 引用展示（验收 A3）
- [ ] 上下文管理：token 预算 + 滑动窗口（§9.5）
- [ ] 密钥存储接入系统钥匙串
- [ ] 安全基线验证（Shell 白名单、端口、密钥）（验收 A6）

**DoD**：对话触发 `knowledge_search` 并展示引用；shell 触发权限确认；审计有记录。

**省 Token 要点**：
- RAG 与 MCP 是相对独立的子模块，**可拆成两个会话**分别推进，减少单会话上下文堆积。
- 安全基线一次性做完并写进 DoD 清单，避免上线前返工。

---

### Phase 3 — Agent 化 + 编程助手（3–4 周）

**目标**：把「工具调用」串成「Agent 闭环」。

**任务**：
- [ ] LangGraph 多轮工具调用闭环（`tool_calls` 回灌 ≥3 轮）（验收 A4）
- [ ] 模型路由与降级（含公网模型非默认、仅显式授权可用）
- [ ] 编程助手 Agent（对齐原型 code 场景）
- [ ] 知识库管理界面（上传、删除、检索历史、引用查看）
- [ ] 上下文摘要压缩节点
- [ ] Agent 评测集搭建（golden set 基线）

**DoD**：多工具连续调用任务（如「总结项目进展」= knowledge_search + filesystem_read）端到端正确。

**省 Token 要点**：
- **评测集先行**：先建 golden set，再改 Agent——用「跑评测」代替「人工反复试错」，一次改对，避免多轮调试。
- Agent 循环逻辑集中在 `agents/` 单目录，改动范围小，可精确投喂该目录上下文。

---

### Phase 4 — 打磨发布（2–3 周）

**目标**：让产物可安装、可交付。

**任务**：
- [ ] Electron ↔ Python 生命周期管理 + PyInstaller 打包
- [ ] 打包分发（macOS/Windows/Linux）+ 自动更新
- [ ] 用户文档 + 内测反馈迭代
- [ ] 发布前验收：按 §2.4 六项硬指标逐项核对，跑全量测试与 eval

**DoD**：干净环境安装包一键运行；README 流程可复现（A1–A6 全项）。

**省 Token 要点**：
- 打包/自动更新是**成熟方案复用**场景，明确要求 AI「引用官方模板」而非从零设计，省去探索成本。
- 验收用清单逐项打勾，产出「一次过」结论，不做无谓重试。

---

## 三、省 Token 总策略（核心）

> 以下 7 条是「最省 Token」的执行原则，覆盖投喂方式、上下文管理、返工预防三个层面。

### 策略 1：按阶段切片投喂，不全量投喂

**不要**每次把两份完整文档整段塞给 AI。正确做法：

```
投喂 = 启动 Prompt（约 1 屏）+ 当前阶段的规格切片（3–6 小节）+ 指向完整文档的路径
```

- 每阶段只给「本阶段任务 + 相关规格」；无关章节一律不给。
- 完整文档保留在项目内，AI 需要时**自己 Read**，而不是你复制给它。

### 策略 2：引用式加载，不复制全文

- 在投喂中写「详见 `DEVELOPMENT-GUIDE.md` §8.2」这样的**路径引用**，让 AI 按需读取。
- 只有当前阶段真正要用的 schema / 表结构 / 事件定义才内联。
- 这样把「全量文档常驻上下文」变成「按需拉取」，上下文从 ~3 万 token 降到 ~2–4 千 token。

### 策略 3：契约一次冻结，重读不重发

- P0 冻结的协议、数据模型、目录结构，后续阶段**只引用，不重述**。
- 若 AI 上下文被清理，让它「读取规格书对应章节」而非「我重新粘贴」。

### 策略 4：DoD 门禁防返工（返工 = 双倍 token）

- 每个阶段有明确完成标准（见上表），未达 DoD 不进入下一阶段。
- 尤其：改 Prompt / 模型 / 分块参数前，**先跑评测再合入**，避免「修好一个、回归一片」。

### 策略 5：独立会话隔离上下文

- 每个阶段（甚至每个子模块）开独立会话，结束时清理。
- 避免「一个超长会话 + 大量历史」——历史是输入 token 的隐形消耗。
- 阶段产物落盘（代码/文档），新会话靠「读产物 + 读规格」续接，不靠上下文记忆。

### 策略 6：批量指令，减少往返

- 一次交互尽量完成一类任务（如「建全部门表 + 建对应 repository + 写单元测试」），而非一条条下指令。
- 减少「多轮对话」本身的开销（每轮都带着全部历史）。

### 策略 7：控制每条指令的规模与明确度

- 每条指令目标单一、验收明确，避免 AI 理解偏差导致重做。
- 明确给出「不要做什么」（对应 Prompt §十一 禁止事项），减少越界尝试浪费 token。

---

## 四、投喂模板（可直接复制）

### 会话启动模板（每个阶段通用）

```text
你是本项目的高级全栈工程师。请完成以下阶段任务。

【当前阶段】Phase N — <阶段名>
【阶段任务】<粘贴该阶段任务清单>
【规格参考】请按需读取项目内文档，不要全文输出：
  - 开发规格书：DEVELOPMENT-GUIDE.md（重点章节：§X、§Y）
  - 指挥 Prompt：DEV-COMMAND-PROMPT.md（约束与禁止事项）
【验收标准】<粘贴该阶段 DoD>
【完成方式】完成后列出产出文件清单 + 自检结果，不要贴大段代码。
```

### 续接会话模板（换会话继续）

```text
继续 Phase N。请先读取：
  1. 已完成产物：<路径>
  2. 规格书 §X
然后从 <上次停下的点> 继续，验收标准不变。
```

---

## 五、预估收益（示意）

> 以下为量级示意，具体因模型与工具而异，但相对关系成立。

| 投喂方式 | 单会话输入量级 | 返工风险 | 总成本相对值 |
|----------|---------------|----------|--------------|
| 全量投喂两份文档 + 单会话跑完全程 | ~30k+ token/轮，历史累积 | 高 | 基准 |
| 按阶段切片 + 引用式加载 + 独立会话 | ~2–4k token/轮 | 低 | 约 1/4–1/3 |

> 说明：省 token 主要来自「输入上下文压缩」与「返工减少」两项，二者叠加是数量级差异，而非线性节省。

---

## 附：与两份主文档的关系

| 本文 | 对应文档 | 说明 |
|------|----------|------|
| 二、阶段任务 | `DEV-COMMAND-PROMPT.md` §七 | 任务清单与 DoD 一致 |
| 二、阶段验收 | `DEVELOPMENT-GUIDE.md` §2.4/§12 | 六项硬指标一致 |
| 三、省 Token 策略 | 两份文档的「投喂方式」 | 本文新增的工程化落地 |
| 四、投喂模板 | `DEV-COMMAND-PROMPT.md` | 把全量 Prompt 改为按需引用 |
